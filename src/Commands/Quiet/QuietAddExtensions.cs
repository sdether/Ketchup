using System;

namespace Ketchup.Async.Quiet {

	public static class QuietAddExtensions {

		//public static KetchupClient Add<T>(this KetchupClient client, string key, T value, Action<Exception> error) {
		//    return client.Add(key, value, null, error);
		//}

		//public static KetchupClient Add<T>(this KetchupClient client, string key, T value, TimeSpan expiration, Action<Exception> error) {
		//    return client.Add(key, value, expiration, null, error);
		//}

		//public static KetchupClient Add<T>(this KetchupClient client, string key, T value, DateTime expiration, Action<Exception> error) {
		//    return client.Add(key, value, expiration, null, error);
		//}
	}
}

